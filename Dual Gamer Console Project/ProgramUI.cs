namespace Dual_Gamer_Console_Project
{
    public class ProgramUI
    {
        public void Run()
        {

        }

        // X * Y ascii characters

        // grid

        // switch
            // 1 "text" = "method(parameters)" (change stats and move to new room)

            // 2 "text" = "method(different params)
    }
}